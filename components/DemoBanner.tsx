export default function DemoBanner() {
  return (
    <div className="fixed inset-x-0 top-0 z-[60] flex h-9 items-center justify-center bg-[#17181c] px-4 text-center text-xs font-medium text-white">
      🚧 Demo build — an audit baseline clone of naano.com, not affiliated with naano. No real data.
    </div>
  );
}
